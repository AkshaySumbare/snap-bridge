{
  "name": "snapbridge",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-device capture sync — clipboard, screenshots, and search",
  "workspaces": [
    "backend",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n backend,web -c blue,green \"npm run dev -w backend\" \"npm run dev -w web\"",
    "dev:backend": "npm run dev -w backend",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w backend && npm run build -w web",
    "lint": "npm run lint -w backend && npm run lint -w web",
    "docs:roadmap": "node scripts/generate-roadmap-docx.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "docx": "^9.5.0"
  }
}
